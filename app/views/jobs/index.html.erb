<% provide(:title, 'Jobs Index') %>

<h1>All Jobs</h1>
<div class="row">
  <div class="span8">
    <ul>
      <% @jobs.each do |job| %>
        <li><%= link_to job.name, job %> .............. $<%= job.total_cost %>  </li>
      <% end %>
    </ul>
  </div>
      
</div>
