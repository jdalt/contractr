<% provide(:title, 'Jobs Index') %>

<h1>All Jobs</h1>
<div class="row">
  <div class="span8">
    <ul>
      <% @jobs.each do |job| %>
        <h3><%= link_to job.name, job %></h3> .............. $<%= job.total_cost %>  </li>
      <% end %>
    </ul>
  </div>
      
</div>
